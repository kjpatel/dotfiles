# Never commit credentials. This repository is public.
*.env
.env
env
.secrets
*.pem
*.key
id_rsa*
id_ed25519*

# Claude Code global instructions: machine-local, never synced.
# They accrue project and personal context and this repo is public.
.claude/
