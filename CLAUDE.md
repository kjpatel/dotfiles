# Project Rules

- After ANY change to files in this repo (especially `zshrc`, `Brewfile`, `gitconfig`, `zprofile`, `install.sh`, or config files), update `README.md` and `LEARNING_GUIDE.md` to reflect those changes before considering the task complete.
